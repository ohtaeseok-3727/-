# 지면 60도 구도

사용자가 승인한 각도는 지면과 시선 사이의 60도이다. 3D 카메라를 도입하지 않고 기존 2.5D 직교 투영에서 표현한다.

- 지면 깊이: screenY = cameraAnchorY + (worldY - cameraY) × sin(60°), 약 0.866.
- 입체 석조물·바위의 높이: screenY에서 height × cos(60°), 즉 height × 0.5를 뺀다.
- 타일 높이, 바닥 무늬, 지면 그림자와 청크 로딩 범위가 같은 지면 깊이 비율을 사용한다.
- 캐릭터·나무·깃발 같은 세워진 그래픽은 빌보드로 유지한다. 캐릭터 시트 크기·체형·색상 변경 없음.
- 가림 순서는 기존 월드 Y/발·밑동 기준 정렬을 유지한다. 충돌과 이동 속도는 월드 좌표 기준으로 유지하므로 화면상 세로 이동 거리는 이전보다 커진다.

설정 위치: SimpleGame/PrototypeWorld.cpp의 GroundViewAngle. 기존 깊이 계수 0.58은 교체했다. 60도는 지면 기준이며 수직선 기준 60도와 다르다.

정적 검토만 수행했으며 빌드·게임 실행 및 화면 확인은 하지 않았다.
