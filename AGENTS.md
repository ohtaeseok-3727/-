# 프로젝트 작업 안내

- 이 프로젝트의 기획·구현·설명 전에 `GAME_DESIGN.md`를 읽고 사용자가 확정한 설정을 따른다.
- 사용자가 새 설정을 확정하거나 기존 설정을 변경하면 `GAME_DESIGN.md`에 반영한다.
- 사용자가 말한 확정 설정과 에이전트의 제안을 구분한다. 제안을 사용자 동의 없이 확정 설정으로 기록하지 않는다.
- 미정 사항을 임의로 확정하지 않는다. 구현상 가정이 필요하면 가정임을 명시한다.
- 빌드와 실행 결과 확인은 사용자가 직접 수행한다. 사용자의 명시적인 요청이 없는 한 빌드하거나 실행하여 결과를 확인하지 않는다. 코드·문서의 정적 검토는 가능하며, 작업 보고 시 실제 수행하지 않은 빌드나 실행 검증을 했다고 표현하지 않는다.

## 코드 구성 원칙

- 줄바꿈을 넉넉히 사용하는 코드 컨벤션을 유지한다. C++ 서식은 루트 `.clang-format`을 따른다. 중괄호는 다음 줄에 두고, 짧은 함수·조건문·반복문도 여러 줄로 펼치며, 함수 정의 사이에는 빈 줄을 둔다. 들여쓰기는 공백 4칸으로 한다.
- 외부 라이브러리(`SimpleGame/Dependencies`)와 자동 생성 데이터(`SimpleGame/CombatVisualData.h`)는 수동 서식 정리 대상에서 제외한다.
- 앞으로 코드는 객체지향 방식으로 작성한다.
- 서로 다른 기능은 각 기능의 책임을 담당하는 클래스·모듈로 분리하고, 별도 파일에 작성한다.
- 분리한 기능은 명확한 공개 메서드나 인터페이스를 통해 연결한다.
- 새 기능을 추가할 때 기존 파일에 서로 다른 책임을 한꺼번에 누적하지 않는다.
- 필요에 따라 STL과 최신 C++ 기능을 최대한 활용한다. 적용할 기능은 프로젝트의 C++ 표준 및 컴파일러 지원 범위를 확인해 선택한다.
- 코드를 작성하거나 수정한 뒤에는 각 파일·클래스의 역할, 모듈 간 연결, 주요 실행 흐름을 사용자가 이해할 수 있도록 설명한다. 사용한 STL이나 최신 C++ 기능의 역할과 선택 이유도 함께 설명한다.

## 스프라이트 필수 지침

- 최신 사용자 지시: 공격 스프라이트는 원본 픽셀을 복사·재사용하지 않고 새로 그린다. 미니 이동 시트를 체형·픽셀 크기·색상의 시각적 기준으로 유지한다.

- 모든 스프라이트 생성·편집 요청에서 반드시 해당 캐릭터의 미니 이동 스프라이트를 고정 기준으로 사용한다. 역동성, 동작 변경, 도트 강조 등 어떤 요청도 이 기준을 완화하는 근거가 아니다. 사용자가 기준 자체를 명시적으로 바꾸기 전까지 유지한다.
- 체형(머리·몸통 크기와 비율, 어깨 폭, 팔다리 길이·굵기), 픽셀 크기, 픽셀 색상 팔레트는 이동 스프라이트와 동일해야 한다. 비슷한 분위기나 대략적인 비율로 대체하지 않는다.
- 기사 기준: `SimpleGame/Assets/Characters/SmallKnight/walk-transparent-v1.png`.
- 마법사 기준: `SimpleGame/Assets/Characters/SmallMage/walk-transparent-v2.png`.
- 모든 생성·편집 프롬프트와 제작 사양에 위 고정 조건을 명시한다. 프롬프트에 썼다는 것만으로 준수했다고 판단하지 않고, 결과를 기준 시트와 직접 대조한다.
- 모든 스프라이트 배경은 실제 알파 채널이 있는 투명 배경이어야 한다. 체크무늬 또는 단색 배경으로 대체하지 않는다.
- 움직이는 동안 신체구조가 변형되거나 팔이 사라지고 중복되거나 비정상적으로 꺾이면 안 된다. 요청한 좌우 손과 동작을 유지한다.
- 사용자가 참조를 해제한 이전 마법사 공격 시트·수정 시안·재사용 실험을 새 제작의 참조로 사용하지 않는다. 최신 동작 정의는 `GAME_DESIGN.md`를 따른다.
- 조건을 충족하지 못한 결과를 완성본으로 적용하거나 준수했다고 보고하지 않는다. 동일한 실패를 일으키는 전체 재생성을 무작정 반복하지 말고, 원본 픽셀 보존 등 제약을 실제로 유지할 수 있는 방법을 검토한다.
